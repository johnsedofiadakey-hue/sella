export default function Home() {
  return (
    <div className="flex flex-1 flex-col items-center justify-center px-6 text-center">
      <p className="text-xs font-semibold uppercase tracking-wide text-ink-muted">
        StormGlide
      </p>
      <h1 className="mt-2 max-w-lg text-4xl font-bold text-forest-dark">
        Your business deserves a real online store.
      </h1>
      <p className="mt-3 max-w-md text-ink-muted">
        Live in 15 minutes · GHS 466/month · First month free.
      </p>
      <p className="mt-8 text-xs text-ink-muted">
        Marketing site ships in Month 2 of the build plan — this is a
        placeholder proving the root domain resolves separately from a store
        subdomain.
      </p>
    </div>
  );
}
